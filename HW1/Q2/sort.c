#include "stdio.h"
#include "math.h"
#include "algorithm"

int main (void)
{
	FILE unsorterd;
	int N I=1;
	scanf(%d,N);
	printf("number of random elements=\n");
	int sorted[N] unsorted[N];
		while(fscanf(unsorted,"%d",unsorted[I])==1)
	{
		fscanf(unsorted,"%d",unsorted[I])=unsorted[I];
		printf("%d",unsorted[4];
	}	 
	return 0;
}
